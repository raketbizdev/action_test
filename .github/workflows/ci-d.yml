name: Named Parallel Jobs

on:
  push:
    branches:
      - dev

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Build Frontend
        run: echo "Building the frontend"

  build-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Build Backend
        run: echo "Building the backend"

  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
        run: echo "Running tests"
